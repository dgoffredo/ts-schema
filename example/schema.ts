// A Person is a whatever.  They're like whatever, this is a comment, and it
// doesn't break my script.  Isn't that grand?
interface /* Here is another comment, like WFT? */ Person {
	firstName: string,
	lastName: string,
	age?: number
}
